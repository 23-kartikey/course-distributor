spring:
  application:
    name: course-distributor
  datasource:
    url: jdbc:mysql://localhost:3307/course_distributor
    username: kartikey
    password: password

    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:  
    hibernate:
      ddl-auto: update
      show-sql: true

    properties:
      hibernate:
      format_sql: true