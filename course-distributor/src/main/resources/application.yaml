spring:
  application:
    name: course-distributor

  datasource:
    url: jdbc:postgresql://localhost:5432/course_distributor
    username: kartikey
    password: password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    # properties:
    #   hibernate:
    #     format_sql: true
    #     jdbc:
    #       time_zone: Asia/Kolkata
    
    # database-platform: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
app:
  jwt-secret: daf66e01593f61a15b857cf433aae03a005812b31234e149036bcc8dee755dbb
  jwt-expiration: 604800000