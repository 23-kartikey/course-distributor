export type RegisterForm = {
    email: string,
    password: string
}

export type LoginForm = {
    usernameOrEmail: string,
    password: string
}

export type DetailsFormType = {
    firstName: string,
    lastName: string,
    username: string, 
    about: string
}